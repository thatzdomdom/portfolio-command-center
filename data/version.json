{"build":55,"note":"Bumped on every deploy. index.html fetches this with a cache-buster and reloads once if the server is ahead — GitHub Pages caches HTML for 600s, which otherwise makes a fresh fix look broken."}
